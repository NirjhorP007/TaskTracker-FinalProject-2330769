# Task Tracker – Final Project

A simple React task tracker application.

## How to Run
1. Install Node.js
2. Run `npm install`
3. Run `npm run dev`

## Example Usage
- Add tasks
- Mark tasks as completed
- Delete tasks
- View task statistics in footer